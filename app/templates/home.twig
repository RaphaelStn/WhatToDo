{% extends 'layout.twig' %}
{% import 'favorite-form.twig' as form%}
{% block title %}
Home
{% endblock %}


{% block content %} 

<section id="TV">
    <div class="title">
        <h1> What To Do </h1>
        <h2> Let us choose for you.</h2>
        <p> Want to play a game? Maybe watch a trending movie, or you could also pick at random a movie, a show or a game.
        To add something to your favorite, you need to register an account. Once connected you will find your favorites on your account page.</p>
    </div>
    <h3><i class="fas fa-fire"></i> Top movies of the week</h3>
    <div class="container-poster container-movie">
        {% for movie in movies %}
            <div class="poster">
                {{form.fav('movies', movie.id, favMovies)}}
                <a href="index.php?p=movie_poster&id={{movie.id}}" class="more">
                     <img src="https://image.tmdb.org/t/p/w500/{{movie.poster_path}}"></img>
                 </a>
                <p>{{movie.title}}</p>
            </div>
        {% endfor %}
    </div>
    <a href="index.php?p=movie_poster" class="btn-random">Pick me a movie</a>
    <h3 ><i class="fas fa-fire"></i> Top TV Show of the week</h3>
    <div class="container-poster container-show">
        {% for show in shows %}       
            <div class="poster">
                {{form.fav('shows', show.id, favShows)}}
                <a href="index.php?p=show_poster&id={{show.id}}" class="more">
                    <img src="https://image.tmdb.org/t/p/w500/{{show.poster_path}}"></img>
                </a>
                <p>{{show.name}}</p>
            </div>
        {% endfor %}
    </div>
    <a href="index.php?p=show_poster" class="btn-random">Pick me a TV Show</a>
</section>
<section id = "game">
    <h3>Top rated games of all time <i class="fas fa-chevron-down"></i></h3>
    <div class="container-game">
        {% for game in games %}
            <div class="poster-game">
                {{form.fav('games', game.id, favGames)}}
                <a href="index.php?p=game_poster&id={{game.id}}" class="more">
                    <img src="{{game.background_image}}"></img>
                </a>
                <p>{{game.name}}</p>
            </div>
        {% endfor %}
    </div>
    <a href="index.php?p=game_poster" class="btn-random">Pick me a movie</a>
</section>
<section id="twitch">
    <p>Finally, you can just chill on Twitch watching a stream, here are the top streamers live right know</p>
    <div class="container-poster container-stream">
    </div>
</section>
{% endblock %}

{% block scripts %}
<script src="scripts/home.js"></script>
{% endblock %}
